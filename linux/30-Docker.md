# Docker on Linux

## Commands
```bash
docker ps
docker images
docker logs CONTAINER
docker exec -it CONTAINER bash
docker inspect CONTAINER
```

## Administration
Learn images, containers, volumes, networks, registries, and Compose.

## Best Practice
Use persistent volumes for data and never store production secrets directly in images.
