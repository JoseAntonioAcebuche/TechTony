# Git and GitHub for Linux Administration

## Common Commands
```bash
git clone REPOSITORY
git status
git add .
git commit -m "Update documentation"
git push origin main
```

## Best Practice
Use meaningful commits, avoid committing secrets, and document infrastructure changes in version control.
