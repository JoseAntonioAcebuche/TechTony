# Package Management

## Fedora / RHEL
```bash
sudo dnf search nginx
sudo dnf install nginx
sudo dnf update
sudo dnf remove nginx
rpm -qa
```

## Debian / Ubuntu
```bash
sudo apt update
sudo apt install nginx
sudo apt upgrade
```

## Best Practices
Keep systems patched, review repositories, and test updates before applying them to critical production systems.
